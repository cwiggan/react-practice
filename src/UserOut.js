import React from 'react';
const userout = (props) => {
    return(
        <div>
            <p>UserName is {props.username}</p>
            <p>Number one on the charts</p>
        </div>
    )
}

export default userout;